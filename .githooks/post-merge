#!/usr/bin/env bash
set -euo pipefail

if git diff-tree -r --name-only --no-commit-id ORIG_HEAD HEAD | grep -q '^package-lock.json$'; then
  echo "package-lock.json changed; run npm install if dependencies are missing."
fi
